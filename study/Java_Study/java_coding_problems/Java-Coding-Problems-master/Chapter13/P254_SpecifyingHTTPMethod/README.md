# Specifying the HTTP Method

Write a program that specifies the HTTP method of a request (e.g., **GET**, **POST**, **PUT** and **DELETE**).

요청의 HTTP 메서드(예: **GET**, **POST**, **PUT** 및 **DELETE**)를 지정하는 프로그램을 작성하세요.
